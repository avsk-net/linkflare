services:
  app:
    build: .
    ports:
      - "9000:9000"
    env_file: .env
    depends_on:
      - redis
    volumes:
      - ./data:/app/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
